// Intentionally empty.
// This app now uses pure AppKit (see ClipHistoryApp.swift). The old SwiftUI
// menu view lived here; it was removed to avoid the SwiftUI macro plugin
// requirement so the app builds with a plain `swiftc` toolchain.
